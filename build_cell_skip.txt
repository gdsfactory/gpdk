# Cells excluded from CI's all-cells DRC build.
# Each has a genuine, pre-existing gdsfactory-core bug that reproduces
# with zero gpdk involvement (calling the function directly against
# gdsfactory.generic_tech.get_generic_pdk() fails identically).
# Already documented and skipped the same way in tests/test_pdk.py's skip_test.
#
# add_pads_bot, add_pads_top: RuntimeError: Routing collision, with the
# default pad_pitch/straight_separation.
add_pads_bot
add_pads_top
